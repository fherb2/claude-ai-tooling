# Status: abgearbeitete Fahrplanschritte

Ausschließlich erledigte Fahrplaneinträge, in der Reihenfolge des Abschlusses. Hier stehen **keine** Entscheidungen — die gehören sofort in das zuständige Kapitel von `implementation_doku.md`.

- **Projektrahmen mit dem Nutzer besprochen und festgelegt** — Vorhaben-Status (eigenständig, drittes Vorhaben neben `chats-export` und `home-.claude-sharing`), Speicherort-Semantik (`skills/` hier ist nur Quelle, wirksame Ladeorte sind `.claude/skills/` bzw. `~/.claude/skills/`), Testverfahren ohne Ablage am Zielort, sowie die Doku-Struktur (volle Drei-Segment-Gliederung, Dateiname `implementation_doku.md`).
- **Offizielle Skill-Dokumentation recherchiert** — [Extend Claude with skills](https://code.claude.com/docs/en/skills), bestätigt das vom Nutzer bereits eigenrecherchierte Ladeverhalten (nur `SKILL.md`-Inhalt lädt bei Aktivierung, keine automatische Mitnahme weiterer Ordnerdateien).
- **Trigger-Testreihe durchgeführt und eingearbeitet** (Schritt 8, ad hoc entstanden und am selben Tag abgeschlossen) — empirische Untersuchung, wann ein Skill von selbst feuert, auf Sonnet 5, Opus 5 und Fable 5; Ergebnisse als Kapitel 1.5/1.6 (Befunde und Testverfahren) und als erste Vorgabe 2.1 (Trigger-Formulierung) in `implementation_doku.md`.
- **Design des Übersetzungs-Skills besprochen** (Schritt 1) — Auslösung mit und ohne Rückfrage, Kalibrierungsfragen, Arbeitsprobe, Codeblock-Behandlung, Umgang mit Eigennamen und wörtlichen Markern.
- **Glossar-Idee eingearbeitet** (Schritt 2) — Terminologie-Glossar mit Umgebungserkennung, damit es nur dort geführt wird, wo es auch auffindbar bleibt.
- **Begriff „stiller Trigger" eingeführt** — Kapitel 1.7: Auslöser, der nicht in der Skill-Description steht, sondern als Absatz in der `CLAUDE.md` des Zielorts liegt.
- **Segment 2 gefüllt** (Schritt 5) — Vorgaben 2.1 (Trigger-Formulierung), 2.2 (Aufbau eines Skill-Ordners samt `CLAUDE-snippet.md`) und 2.3 (Aufbau der README).
- **Erste drei Skills als Entwurf angelegt und in Segment 3 dokumentiert** (Schritt 6) — `translation-task` (ohne stillen Trigger), `parallel-sessions` und `software-dev-doc-fh` (beide mit); dazu Kapitel 3.4 als festgehaltene, noch nicht umgesetzte Idee „Softwareaufgabe erkennen".
- **Nachmessung der Trigger-Befunde, Korrektur eingearbeitet** — Ausschlusstest mit getauschter `description` bei sonst identischem Aufbau: Die Description ist der stärkste Hebel, nicht der Trigger-Text; eine schwache Description machte den Trigger auf Sonnet unwirksam, während Opus und Fable sie kompensierten. Damit relativiert: das frühere „eigenschaftsförmige Trigger feuern nie" (galt bei schwacher Description). Ausgeschlossen als Ursache: Umfang der `CLAUDE.md` und konkurrierende Handlungsanweisung im Prompt. Eingearbeitet in 1.5, 1.6, 2.1 und 3.2; Trigger von `parallel-sessions` auf die ereignisförmige, zusätzlich geankerte Fassung umgestellt.
- **Nicht-interaktiven Testweg erprobt und dokumentiert** — `claude -p` mit `--output-format json` bzw. `stream-json --verbose` in einem isolierten Wegwerf-Projekt; der Skill-Aufruf ist dabei als `tool_use`-Ereignis im Stream sichtbar. Eingearbeitet in Kapitel 1.6. Dazu die Längengrenze der `description` recherchiert (1.536 Zeichen, Kürzung von hinten) und als Formulierungsregel in 2.1 ergänzt.
- **README erstellt** (Schritt 4) — vollständige Anwenderdokumentation mit Zweck, Installationsweg samt Behandlung der stillen Trigger und Einzelbeschreibung der Skills; Lizenzfrage dabei geklärt und auf CC0 festgelegt (Anthropic macht keine Vorgaben, recherchiert am 14. August 2026).
